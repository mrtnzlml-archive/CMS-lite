<nav class="navbar navbar-default">
	<div class="container-fluid">
		<div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
			<ul class="nav navbar-nav">

				<li {ifCurrent :Options:Options:default}class="active"{/ifCurrent}>
					<a href="{plink :Options:Options:default}">
						Rozšíření
					</a>
				</li>

				{foreach $categories as $category}
					<li {ifCurrent :Options:Options:$category->name}class="active"{/ifCurrent}>
						<a href="{plink :Options:Options:$category->name}">
							{_$category->fullName}
							{ifCurrent :Options:Options:$category->name}
								<span class="sr-only"> ({_aktuální})</span>
							{/ifCurrent}
						</a>
					</li>
				{/foreach}

			</ul>
		</div>
	</div>
</nav>
